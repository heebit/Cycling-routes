import { extendTheme } from "@chakra-ui/react";
const theme = extendTheme({
  fonts: {
    body: "Oswald, sans-serif",
  },
});
export default theme;

//import { extendTheme } from '@chakra-ui/react'; const theme = extendTheme({ fonts: { heading: 'MyCustomFont, sans-serif', body: 'MyCustomFont, sans-serif', }, }); export default theme;
